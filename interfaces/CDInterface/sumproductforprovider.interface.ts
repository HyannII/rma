export interface ISumProductForProvider {
    providers_id: number,
    name: string,
    description: string,
    total_products: string,
    total_pending_products: string,
    total_completed_products: string
}